import numpy as np
import nnfs
from nnfs.datasets import spiral_data

nnfs.init()

# use "py -m ensurepip --default pip" to install pip
# use "py -m pip install numpy" to install numpy (so happy I figured this out with google or help lol)
# use "py -m pip install nnfs" to install nnfs package
# use "py -m pip install transformers datasets" to install datasets
# use "py -m pip install tensorflow" to install tensorflow

